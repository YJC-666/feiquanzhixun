cmake_minimum_required(VERSION 3.0.2)
project(jie_octomap)

find_package(catkin REQUIRED COMPONENTS
  geometry_msgs
  jie_map_msgs
  rospy
  sensor_msgs
  std_msgs
  visualization_msgs
)

catkin_package(
  CATKIN_DEPENDS
    geometry_msgs
    jie_map_msgs
    rospy
    sensor_msgs
    std_msgs
    visualization_msgs
)

catkin_install_python(
  PROGRAMS
    scripts/octomap_web_backend.py
  DESTINATION ${CATKIN_PACKAGE_BIN_DESTINATION}
)

install(DIRECTORY launch config web
  DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION}
)